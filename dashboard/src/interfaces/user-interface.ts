export interface iUser {
	exp: number,
	iat: number,
	id: number,
	user: string,
	uuid: number,
	email: string,
	image_url: string
}

// let scores: Record<string, number> = {};
// let team = new Map<string, number>();
// team.set("bill", 10);

// export enum CardType {
// 	AMEX = "AMEX",
// 	VISA = "VISA"
// }